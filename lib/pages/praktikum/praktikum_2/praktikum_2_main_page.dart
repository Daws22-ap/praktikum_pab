import 'package:flutter/material.dart';

class Praktikum2MainPage extends StatelessWidget {
  const Praktikum2MainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Praktikum 2')),
      body: const Center(child: Text('Selamat datang di Praktikum 2!')),
    );
  }
}
